// Generated by CoffeeScript 1.10.0

/*
 * Wand
Created by Aaron Delaney for a DCU Assignment

http://github.com/devoxel | http://twitter.com/devoxel

When I get permission to release open-source it,
you'll be able to find it at my github!
 */

(function() {
  var Config, EditorController, EditorModel, EditorView, config, editor_controller, editor_model, editor_view;

  Config = (function() {
    function Config() {
      this.debug = true;
    }

    return Config;

  })();

  EditorModel = (function() {
    function EditorModel(config1) {
      this.config = config1;
      if (this.config.debug) {
        console.log("+ Iniating Model");
      }
    }

    return EditorModel;

  })();

  EditorView = (function() {
    function EditorView(config1) {
      this.config = config1;
      if (this.config.debug) {
        console.log("+ Initiating EditorView");
      }
    }

    return EditorView;

  })();

  EditorController = (function() {
    function EditorController(config1) {
      this.config = config1;
      if (this.config.debug) {
        console.log("+ Initiating EditorController");
      }
    }

    return EditorController;

  })();

  config = new Config();

  editor_model = new EditorModel(config);

  editor_view = new EditorView(config);

  editor_controller = new EditorController(config);

}).call(this);

//# sourceMappingURL=main.js.map
